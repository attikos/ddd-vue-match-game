export interface CardPresenter {
    cardImageAsset: string[]
    currentCards: string[]
}

export function CardPresenter(): any {
    // const { cardImageAsset, currentCards } = useCardStore()

    return {
        // cardImageAsset,
        // currentCards,
    };
}
